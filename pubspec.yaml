name: secretary
description: A sophisticated tool for managing queues of asynchronous tasks, with a stream interface, strong typing and lots of helpful options.
version: 1.2.0
repository: https://github.com/alexobviously/secretary

environment:
  sdk: '>=3.8.0 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
dependencies:
  elegant: ^0.0.7
